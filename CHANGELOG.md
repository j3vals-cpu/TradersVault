## v2.6.1 — Hotfix

- Fixed copier panel crash that broke window controls (X button, minimize, etc.)
- Removed unsafe function reassignment pattern
- Wrapped copier init in try-catch so it can never break other panels
